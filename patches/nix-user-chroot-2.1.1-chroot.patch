diff --git a/src/main.rs b/src/main.rs
--- a/src/main.rs
+++ b/src/main.rs
@@ -20,6 +20,20 @@ mod mkdtemp;
 
 const NONE: Option<&'static [u8]> = None;
 
+#[derive(Debug, Clone, Copy, PartialEq, Eq)]
+enum RootMethod {
+    Pivot,
+    Chroot,
+}
+
+impl RootMethod {
+    fn parse(value: &str) -> Option<Self> {
+        match value {
+            "pivot" => Some(Self::Pivot),
+            "chroot" => Some(Self::Chroot),
+            _ => None,
+        }
+    }
+}
+
 fn bind_mount(source: &Path, dest: &Path) {
     if let Err(e) = mount(
         Some(source),
@@ -317,7 +331,13 @@ impl<'a> RunChroot<'a> {
         }
     }
 
-    fn run_chroot(&self, cmd: &str, args: &[String], path_config: Option<PathConfig>) {
+    fn run_chroot(
+        &self,
+        root_method: RootMethod,
+        cmd: &str,
+        args: &[String],
+        path_config: Option<PathConfig>,
+    ) {
         let cwd = env::current_dir().expect("cannot get current working directory");
 
         let uid = unistd::getuid();
@@ -547,25 +567,32 @@ impl<'a> RunChroot<'a> {
             )
         });
 
-        // chroot
-        unistd::pivot_root(self.rootdir, &nix_mount).unwrap_or_else(|err| {
-            panic!(
-                "pivot_root({}, {}): {}",
-                self.rootdir.display(),
-                &nix_mount.display(),
-                err
-            )
-        });
-
-        // mount the store and hide the old root we fetch nixdir under the old root
-        let nix_store = nix_root.join(self.nixdir);
-        mount(
-            Some(&nix_store),
-            "/nix",
-            Some("none"),
-            MsFlags::MS_BIND | MsFlags::MS_REC,
-            NONE,
-        )
-        .unwrap_or_else(|_| panic!("failed to bind mount {} to /nix", nix_store.display()));
+        match root_method {
+            RootMethod::Pivot => {
+                unistd::pivot_root(self.rootdir, &nix_mount).unwrap_or_else(|err| {
+                    panic!(
+                        "pivot_root({}, {}): {}",
+                        self.rootdir.display(),
+                        &nix_mount.display(),
+                        err
+                    )
+                });
+
+                // The pivot path retains the upstream behaviour: the old root is
+                // temporarily reachable below /nix, so re-bind the physical Nix
+                // directory onto /nix before continuing.
+                let nix_store = nix_root.join(self.nixdir);
+                mount(
+                    Some(&nix_store),
+                    "/nix",
+                    Some("none"),
+                    MsFlags::MS_BIND | MsFlags::MS_REC,
+                    NONE,
+                )
+                .unwrap_or_else(|_| panic!("failed to bind mount {} to /nix", nix_store.display()));
+            }
+            RootMethod::Chroot => {
+                // The store is already bind-mounted at <rootdir>/nix. After
+                // chroot(rootdir), that mount is directly visible as /nix.
+                unistd::chroot(self.rootdir)
+                    .unwrap_or_else(|err| panic!("chroot({}): {}", self.rootdir.display(), err));
+            }
+        }
 
         env::set_current_dir("/").expect("cannot change directory to /");
 
@@ -629,13 +656,38 @@ fn main() {
         .init();
 
     let args: Vec<String> = env::args().collect();
-    if args.len() < 3 {
-        eprintln!("Usage: {} <nixpath> <command>\n", args[0]);
-        process::exit(1);
-    }
+    let (root_method, nixpath_idx, command_idx) = if args.get(1).map(String::as_str)
+        == Some("--root-method")
+    {
+        if args.len() < 5 {
+            eprintln!(
+                "Usage: {} [--root-method pivot|chroot] <nixpath> <command>\n",
+                args[0]
+            );
+            process::exit(1);
+        }
+        let root_method = RootMethod::parse(&args[2]).unwrap_or_else(|| {
+            eprintln!(
+                "invalid root method '{}': expected pivot or chroot",
+                args[2]
+            );
+            process::exit(2);
+        });
+        (root_method, 3, 4)
+    } else {
+        if args.len() < 3 {
+            eprintln!(
+                "Usage: {} [--root-method pivot|chroot] <nixpath> <command>\n",
+                args[0]
+            );
+            process::exit(1);
+        }
+        (RootMethod::Pivot, 1, 2)
+    };
 
     let rootdir = mkdtemp::mkdtemp("nix-chroot.XXXXXX")
         .unwrap_or_else(|err| panic!("failed to create temporary directory: {err}"));
 
-    let nixdir = fs::canonicalize(&args[1])
-        .unwrap_or_else(|err| panic!("failed to resolve nix directory {}: {}", &args[1], err));
+    let nixdir = fs::canonicalize(&args[nixpath_idx]).unwrap_or_else(|err| {
+        panic!(
+            "failed to resolve nix directory {}: {}",
+            &args[nixpath_idx], err
+        )
+    });
@@ -684,7 +736,12 @@ fn main() {
     match unsafe { fork() } {
         Ok(ForkResult::Parent { child, .. }) => wait_for_child(&rootdir, child),
         Ok(ForkResult::Child) => {
-            RunChroot::new(&rootdir, &nixdir).run_chroot(&args[2], &args[3..], path_config)
+            RunChroot::new(&rootdir, &nixdir).run_chroot(
+                root_method,
+                &args[command_idx],
+                &args[command_idx + 1..],
+                path_config,
+            )
         }
         Err(e) => {
             eprintln!("fork failed: {e}");
