experimental-features = nix-command flakes
# Bootstrap starts conservatively with the sandbox disabled, then enables it
# after a real sandboxed build probe succeeds on the current host.
sandbox = false
# Never silently fall back to an unsandboxed build after sandboxing was enabled.
sandbox-fallback = false
