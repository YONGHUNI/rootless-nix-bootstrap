experimental-features = nix-command flakes
# Rootless single-user builds cannot rely on the normal root/build-user sandbox.
# Project inputs are still pinned by flake.lock; doctor reports this explicitly.
sandbox = false
